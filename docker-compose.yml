version: "3.8"

services:
  mariadb:
    image: mariadb:10.6
    container_name: mystudy-db
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: S6868645s
      MARIADB_DATABASE: mystudy-db
      MARIADB_USER: studyuser
      MARIADB_PASSWORD: S6868645s
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data: